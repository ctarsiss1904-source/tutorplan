import Link from "next/link";import {tutorPages,region} from "@/lib/data";export default function Home(){const pages=tutorPages();const picks=pages.slice(0,8);return <><section className="hero"><h1>지역의 학습 흐름을<br/>차분하게 살펴보는 과외</h1><p>현재 공개 준비가 완료된 지역별 일반 과외 페이지를 안내합니다.</p><Link href="/regions">지역별 과외 찾기</Link></section><section><h2>주요 지역 탐색</h2><div className="grid">{picks.map(p=><article className="card" key={p.page_id}><Link href={p.canonical_url.replace("https://tutorplan.co.kr","")}>{p.content.h1}</Link><p>{region(p.region_id)?.sido}</p></article>)}</div></section></>}
