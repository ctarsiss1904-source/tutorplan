import {tutorPages} from "@/lib/data";export const dynamic='force-static';export function GET(){const urls=tutorPages().map(p=>`<url><loc>${p.canonical_url}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`,{headers:{'Content-Type':'application/xml'}})}
